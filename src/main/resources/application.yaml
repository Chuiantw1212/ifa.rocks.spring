# ... (existing config)

# LINE LIFF Integration Settings
line:
  liff:
    # Your LIFF Channel ID, should be set via environment variable in production
    channel-id: ${LINE_LIFF_CHANNEL_ID:?LINE_LIFF_CHANNEL_ID is not set}

# ... (rest of the config)
