<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.en_chu.calculator_api_spring.mapper.UserRealEstateMapper">

    <resultMap id="BaseResultMap" type="com.en_chu.calculator_api_spring.entity.UserRealEstate">
        <id column="id" property="id" />
        <result column="firebase_uid" property="firebaseUid" />
        <result column="name" property="name" />
        <result column="age" property="age" />
        <result column="size" property="size" />
        <result column="price_per_ping" property="pricePerPing" />
        <result column="total_price" property="totalPrice" />
        <result column="assessed_value" property="assessedValue" />
        <result column="holding_tax_rate" property="holdingTaxRate" />
        <result column="actual_holding_cost" property="actualHoldingCost" />
        <result column="loan_amount" property="loanAmount" />
        <result column="interest_rate" property="interestRate" />
        <result column="usage_type" property="usageType" />
        <result column="monthly_rent" property="monthlyRent" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, firebase_uid, name, age, size, price_per_ping, total_price,
        assessed_value, holding_tax_rate, actual_holding_cost, loan_amount,
        interest_rate, usage_type, monthly_rent, created_at, updated_at
    </sql>

    <select id="selectByUid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_real_estates
        WHERE firebase_uid = #{firebaseUid}
        ORDER BY created_at DESC
    </select>

    <select id="selectByIdAndUid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_real_estates
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_real_estates (
            firebase_uid, name, age, size, price_per_ping, total_price,
            assessed_value, holding_tax_rate, actual_holding_cost, loan_amount,
            interest_rate, usage_type, monthly_rent
        ) VALUES (
            #{firebaseUid}, #{name}, #{age}, #{size}, #{pricePerPing}, #{totalPrice},
            #{assessedValue}, #{holdingTaxRate}, #{actualHoldingCost}, #{loanAmount},
            #{interestRate}, #{usageType}, #{monthlyRent}
        )
    </insert>

    <update id="update">
        UPDATE user_real_estates
        SET
            name = #{name},
            age = #{age},
            size = #{size},
            price_per_ping = #{pricePerPing},
            total_price = #{totalPrice},
            assessed_value = #{assessedValue},
            holding_tax_rate = #{holdingTaxRate},
            actual_holding_cost = #{actualHoldingCost},
            loan_amount = #{loanAmount},
            interest_rate = #{interestRate},
            usage_type = #{usageType},
            monthly_rent = #{monthlyRent},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </update>

    <delete id="deleteByIdAndUid">
        DELETE FROM user_real_estates
        WHERE id = #{id} AND firebase_uid = #{firebaseUid}
    </delete>

    <delete id="deleteByUid">
        DELETE FROM user_real_estates
        WHERE firebase_uid = #{firebaseUid}
    </delete>

</mapper>
