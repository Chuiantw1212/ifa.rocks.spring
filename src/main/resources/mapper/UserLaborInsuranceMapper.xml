<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.en_chu.calculator_api_spring.mapper.UserLaborInsuranceMapper">

    <resultMap id="BaseResultMap" type="com.en_chu.calculator_api_spring.entity.UserLaborInsurance">
        <id column="id" property="id" />
        <result column="firebase_uid" property="firebaseUid" />
        <result column="expected_claim_age" property="expectedClaimAge" />
        <result column="average_monthly_salary" property="averageMonthlySalary" />
        <result column="insurance_seniority" property="insuranceSeniority" />
        <result column="predicted_remaining_life" property="predictedRemainingLife" />
        <result column="predicted_monthly_annuity" property="predictedMonthlyAnnuity" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, firebase_uid, 
        expected_claim_age, average_monthly_salary, insurance_seniority, 
        predicted_remaining_life,
        predicted_monthly_annuity, created_at, updated_at
    </sql>

    <select id="selectByUid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM user_labor_insurances
        WHERE firebase_uid = #{firebaseUid}
    </select>

    <select id="existsByUid" resultType="boolean">
        SELECT EXISTS (SELECT 1 FROM user_labor_insurances WHERE firebase_uid = #{firebaseUid})
    </select>

    <update id="updateByUid">
        UPDATE user_labor_insurances
        SET
            expected_claim_age = #{expectedClaimAge},
            average_monthly_salary = #{averageMonthlySalary},
            insurance_seniority = #{insuranceSeniority},
            predicted_remaining_life = #{predictedRemainingLife},
            predicted_monthly_annuity = #{predictedMonthlyAnnuity},
            updated_at = CURRENT_TIMESTAMP
        WHERE firebase_uid = #{firebaseUid}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_labor_insurances (
            firebase_uid,
            expected_claim_age,
            average_monthly_salary,
            insurance_seniority,
            predicted_remaining_life,
            predicted_monthly_annuity
        ) VALUES (
            #{firebaseUid},
            #{expectedClaimAge},
            #{averageMonthlySalary},
            #{insuranceSeniority},
            #{predictedRemainingLife},
            #{predictedMonthlyAnnuity}
        )
    </insert>

    <delete id="deleteByUid">
        DELETE FROM user_labor_insurances
        WHERE firebase_uid = #{firebaseUid}
    </delete>

</mapper>